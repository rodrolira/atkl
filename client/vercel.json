{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],
    "buildCommand": "pnpm vite build",
    "installCommand": "pnpm install",
    "outputDirectory": "dist"
}