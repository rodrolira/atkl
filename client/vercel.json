{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],
    "buildCommand": "npx vite build",
    "installCommand": "pnpm install",
    "outputDirectory": "dist"
}